query Login($email: String!, $password: String!) {
  login(dto: {email: $email, password: $password}) {
    token,
    user {
      id,
      username,
      email,
      createdAt,
      updatedAt,
    }
  }
}

